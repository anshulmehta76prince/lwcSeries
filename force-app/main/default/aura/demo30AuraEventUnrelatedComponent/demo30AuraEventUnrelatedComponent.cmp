<aura:component implements="flexipage:availableForAllPageTypes,force:appHostable">
    <aura:attribute name="selectedContact" type="String"/>
    <c:auraPubsub aura:id="aurapubsubmodule" onready="{!c.handleEvent}"/>
    <lightning:card title="Demo 30 Pass Data to Aura Component which is not in hierarchy">
        The slected Contact Name is {!v.selectedContact}
    </lightning:card>
</aura:component>	